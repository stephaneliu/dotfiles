return {
  "nvim-neotest/neotest",
  opts = {
    adapters = {
      -- ["neotest-rspec"] = {
      --   rspec_cmd = function()
      --     return vim.tbl_flatten({ "dip", "rspec" })
      --   end
      -- }
    }
  }
}
